plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file("../key.properties")
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(FileInputStream(keystorePropertiesFile))
}

android {
    namespace = "com.elcadi.elcadi"
    compileSdk = 35
    ndkVersion = "29.0.13113456"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.elcadi.elcadi"
        minSdk 23
        targetSdk 35
        versionCode = 9
        versionName = "app"
    }

    // Correct signingConfigs declaration (only once)
    signingConfigs {
        // debug {
        //     storeFile file("C:/Users/Khorshed/.android/debug.keystore")
        //     storePassword "android"
        //     keyAlias "AndroidDebugKey"
        //     keyPassword "android"
        // }
        release {
            storeFile file("D:/Dev/Projects/Eelecadi/Elecadi/android/upload-keystore.jks")
            storePassword "P@ssw0rd"
            keyAlias "upload"
            keyPassword "P@ssw0rd"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            // Reference to the correct signing config
            signingConfig signingConfigs.release
        }
    }


    buildToolsVersion '35.0.1'
}

flutter {
    source = "../.."
}
